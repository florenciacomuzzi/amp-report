{
  "buildCommand": "cd client && npm install && ./node_modules/.bin/react-scripts build",
  "outputDirectory": "client/build",
  "installCommand": "echo 'Skipping install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}